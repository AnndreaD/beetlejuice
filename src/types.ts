export type QuestionObject = {
    text: string;
    category: string;
    language: string;
};

export type ClaimObject = {
    text: string;
    category: string;
    language: string;
};

export type dropdownItems = {
    value: number;
    label: string;
};
